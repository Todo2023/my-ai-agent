# やることリスト

2026年8月13日時点。手が空いたときに上から順に。
チェックを付けたら、この行を消さずに `- [x]` にしておくと進み具合が分かる。

---

## いますぐ（15分・費用ゼロ・カード登録なし）

マッチングの登録フォームと管理画面は書けているが、**まだどこにもつながっていない**。
つなぐのはここだけ。手順の詳細は [`hp/match/README.md`](hp/match/README.md) の「つなぐ手順」。

- [ ] Supabase で新しいプロジェクトを作る（無料プラン。カード登録は要らない）
- [ ] SQL Editor に [`supabase/schema.sql`](supabase/schema.sql) を貼り付けて実行する
- [ ] 続けて、自分のメールアドレスを管理者に入れる
      `insert into admins (email, note) values ('自分のアドレス', '代表');`
      ※ **これを忘れると、自分でも管理画面に入れない**
- [ ] Project Settings > Data API から URL と anon キーを控える
- [ ] [`hp/match/config.js`](hp/match/config.js) の2行に貼る → push
- [ ] Authentication > URL Configuration の Redirect URLs に管理画面のURLを足す
      `https://todo2023.github.io/my-ai-agent/hp/match/admin/`

### つないだ直後に必ず確認すること

- [ ] **自分で1件テスト登録してみる**（体験を自分で確かめる意味もある）
- [ ] Table Editor に行が増えている
- [ ] **anon キーで `select * from profiles` を叩いて 0 行**
      ← いちばん大事。ここが漏れていたら他は全部無意味になる
- [ ] 管理画面にログインできる／`admins` にないアドレスでは入れない

ここまでで **登録の受け付けと管理画面が動く。費用はゼロのまま。**

---

## 次（Claude API・ここから有料）

- [ ] **先に Anthropic のコンソールで月額上限を設定する**（推奨 $10 ≒ 1,500円）
      ← 順番が大事。キーを作る前に上限を入れておくこと
- [ ] APIキーを発行して、Supabase の secrets に入れる
      `supabase secrets set ANTHROPIC_API_KEY=sk-ant-...`
      ※ リポジトリにも `config.js` にも絶対に書かない
- [ ] `supabase functions deploy generate-matches`
      ※ Edge Function の型チェックだけ手元で未実施。ここで初めてエラーが出る可能性がある

### 勧めたいこと：APIを使う前に、無料で精度を確かめる

大学助教のデータが揃ったら、**いきなりAPIを回さず**、
[`supabase/functions/generate-matches/index.ts`](supabase/functions/generate-matches/index.ts) の
`SYSTEM`（プロンプト）と2人分のプロフィールを Claude.ai に手で貼って、出力を見る。

- 費用ゼロで、プロンプトの当たり外れが分かる
- 直したくなったら、その場で試せる（デプロイし直さなくていい）
- 納得してからAPIに移す

CLAUDE.md の「次のアクション」にも同じことが書いてある。**これを飛ばさないほうがいい。**

- [ ] Claude.ai で手動検証 → プロンプトを直す
- [ ] 良さそうなら、本番で1回だけ実行（$0.05〜0.15）。出てきた理由文の事実が合っているか確認

---

## 判断が要ること（私の推し）

| | 私の推し | 理由 |
| --- | --- | --- |
| **どのモデルを使うか** | まず **Opus 5** のまま。良い出力が出たら Sonnet 5 に落として比較 | 最初から安いモデルだと、出来が悪いときに「モデルのせい」か「プロンプトのせい」か切り分けられない。上限の品質を先に見ておくと判断が早い。Sonnet 5 は単価が半分以下（$3/$15）なので、質が同等ならそのまま乗り換え |
| **メール送信の自動化** | **当面 `mailto:` のまま**（いまの作り） | 費用ゼロ。件数が少ないうちは、送信前に必ず自分の目を通せるほうが事故が起きない。週10件を超えてから Resend 等を検討すれば十分 |
| **登録者の集め方** | フォームのURLを個別に送る | `noindex` にしてあるので検索からは来ない。それでいい。最初は数人で十分 |
| **`match/` の公開** | `noindex, nofollow` は**公開後も外さない** | 個人情報を入れるページ。検索に出す利点がない |
| **助教への依頼文** | 所要時間（5〜10分）と、**セクション4（任意）が精度を左右する**ことを書き添える | あそこが空だと、肩書きマッチングにしかならない。任意だが、実質そこが本体 |

---

## 気づいた宿題（急がないが、いずれ）

- [ ] **`hp/README.md` の参照先の食い違い**
      「料金は CLAUDE.md の想定レンジの真ん中」とあるが、リポジトリの `CLAUDE.md` は費用ルールの文書で、
      営業支援AIの想定レンジは載っていない。どちらかに寄せる
- [ ] **問い合わせ先メールアドレスが未設定**（`hp/app.js` の `CONTACT_EMAIL` が空）
      いま会社サイトの問い合わせを押しても「デモ版のため送信しません」と出るだけ。取りこぼしている
- [ ] **会社概要の `（要記入）` 4項目**（設立／所在地／代表社員／連絡先）。公開の前提
- [ ] **登録内容の削除依頼への対応手順を決める**
      フォームに「削除のご希望はいつでも承ります」と書いた。当面は Table Editor から消す運用でよいが、
      誰がどう受けるかを決めておく
- [ ] 管理画面のログインはタブを閉じると切れる（毎回メールのリンクから入り直す）。
      不便なら、有効期間を延ばすか記憶方式を変える
- [ ] `advisory/` の進め方・料金が空のまま
- [ ] `seo/` の料金（5万／10万）と `sales-ai/` の料金・モニター条件が仮のまま

---

## いまの費用

**ゼロ。** 配信は GitHub Pages、外部の読み込みなし、Claude API は未接続。

課金の決まりは [`CLAUDE.md`](CLAUDE.md) にある。
**有料になる操作の前は、必ず止めて相談する。**
