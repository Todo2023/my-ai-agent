# CLAUDE.md — AIエージェント構築カリキュラム（my-ai-agent）

## このリポジトリについて

- **目的**：AX案件に対応できる状態を、案件が本格化する前に作る。「作れること」ではなく、**設計の切り分け・失敗の切り分け・数値で語れること**の3点を身につけるための実践リポジトリ。
- **進め方**：`README.md` に定義された**5つのゲート**（動かす → 壊す → 構造化する → 測る → 説明する）を順に通過する。期限なし・各自のペース。ただし通過順序は固定。
- **現状**：題材ごとに3つのエージェント実装が同居している。うち `invoice_agent` がゲート1〜5まで通過済みで、リファレンス実装にあたる。

### 意思決定の原則

- 最終判断は必ず人間（Todo）が行う。Claudeは**決定材料を揃えるところまで**が仕事。
- 迷う項目は選択肢を2〜3個に絞って確認を仰ぐ（プルダウン形式が好まれる）。
- 特に**設計判断（LLMに任せる範囲、ゲートの通過可否、成功の定義）は勝手に決めない**。これはカリキュラムの学習対象そのものであり、Claudeが先回りして決めると学びが消える。

---

## ディレクトリ構成

| ディレクトリ | 題材 | 到達ゲート | 依存管理 |
| --- | --- | --- | --- |
| `invoice_agent/` | 自然文の指示から請求書PDFを作る | ゲート5まで通過済み | uv |
| `meeting_agent/` | Googleカレンダー＋Chatworkで Meet URL を共有 | ゲート1〜2相当 | uv |
| `travel_agent/` | 対話形式で旅行プランを提案 | ゲート1相当 | uv |

依存管理は3題材とも **uv に統一**（各ディレクトリの `pyproject.toml` / `uv.lock`）。ルート `README.md` 1.5節で uv を指定しているため、それに合わせている。`meeting_agent` / `travel_agent` の `requirements.txt` は、uv を入れていない人向けの互換用に残してあるが、**依存を追加・更新するときは `pyproject.toml` 側を正とし、`requirements.txt` も揃える**。

`README.md`（ルート）はカリキュラム本体。ゲートの通過条件・レビュー制度・参考資料はすべてここに書かれている。**作業前に該当ゲートの節を読むこと。**

---

## 実行方法

### invoice_agent

```bash
cd invoice_agent
uv run agent.py "A社の請求書を作って"          # ゲート1版：LLMにツールを選ばせる素朴な実装
uv run graph_agent.py "A社の請求書を作って"    # ゲート3版：LangGraphワークフロー（現行の本命）
uv run eval_run.py <モデル名>                  # ゲート4版：評価データセット22件を実行
uv run gate2_run.py                            # ゲート2版：失敗ケース30件の一括実行
uv run trace_export.py                         # ゲート5版：実行トレースを docs/trace_data.js に書き出す
```

- **`agent.py` は捨てずに残す。** ゲート2の失敗分析の対象そのものであり、`graph_agent.py` との対比が学習資産。「古いから消す」という判断はしない。
- 評価結果は `eval_results_<モデル名>.json` に生データを残す。サマリだけ書き換えて生データを消さない。

### 本番の請求書を作るとき

- 請求データは**顧客ごと・月ごとのExcel**（`<顧客の略称>_<YYYYMM>.xlsx`。「請求データ」「顧客マスタ」の2シート）。
- **請求先の対応**：Vastraの業務の請求先は**大谷 晃巨**であり、**Vastra宛の請求書は発行しない**。`vastra_202608.xlsx` は過去の記録として残してあるが、これを元に新しい請求書を作らないこと。
- 請求書番号は `invoice_counter.json`（`.gitignore` 済み）で月ごとに採番する。**このファイルは実行した環境ごとに独立**なので、同じ月の請求書を複数の環境で作ると番号が重複する。**同じ月の分は同じ場所でまとめて作る。**
- 金額・品目・日付を推測で埋めない。前月分を流用するときは、流用したことと変更点を必ず人に伝える。

### 説明用の画面（ゲート5）

`docs/agent_office.html` は、3題材の動きを1画面で見せるデモ用のHTML（依存なし・ブラウザで開くだけ）。`trace_export.py` が出力した `docs/trace_data.js` があれば請求書部が実ログに切り替わり、無ければ同梱の再現データで動く。**実ログは実データを含みうるのでコミットしない**（`.gitignore` 済み）。

### meeting_agent / travel_agent

```bash
cd meeting_agent
uv run python demo.py          # APIキー不要のデモ（Google/Chatwork/Geminiを一切叩かない）
uv run --group dev pytest      # ネットワークもAPIキーも不要
uv run python cli.py           # 本番（GEMINI_API_KEY / CHATWORK_API_TOKEN が必要）
```

```bash
cd travel_agent
uv run python cli.py
```

### APIキー

- すべて `.env` から `python-dotenv` で読み込む（`GEMINI_API_KEY` など）。
- **`.env` の中身をコミットしない・チャットに貼らない・PR本文に書かない。** 参照が必要なときは変数名だけを書く。
- 無料枠はレート制限（429）に当たりやすい。リトライ処理は既存実装（`agent.py` の `max_retries`）の方式に合わせる。

---

## 設計ルール（このカリキュラムの中核）

`invoice_agent/graph_agent.py` の冒頭コメントに、ゲート2で見つかった問題と、それに対する設計上の対応が対応付けて書かれている。**新しいエージェントを書くときも、この方針を踏襲する。**

1. **LLMの仕事は情報抽出に限定する。** 実行するツールをLLM自身に選ばせない。ユーザーに見せる最終的な文章は必ずコード側で組み立てる（ハルシネーションによる嘘の完了報告を構造的に防ぐため）。
2. **表記ゆれの吸収・計算・出力生成は決定的なコードで固める。** 「全部LLMにやらせる」と精度もコストも悪化する。
3. **不可逆な操作（PDF確定、メッセージ送信、削除、購入）の前には必ず人間の承認ゲートを置く。** LangGraph の `interrupt` / CLIの `[y/N]` 確認がその実装。ここを外す変更は絶対にしない。
4. **曖昧なら推測せずに聞き返す。** 顧客名が特定できない、未対応機能を要求された、といったケースは実行前に人へ。
5. **状態はチェックポイントに保存する。** 途中で失敗しても最初からやり直しにならないこと（`langgraph-checkpoint-sqlite`）。
6. **「なぜここをLLMに任せなかったか」を説明できる状態を保つ。** 説明できない変更は入れない。

---

## コード・ドキュメントの書き方

- **コメントとdocstringは日本語。** 既存ファイルは「何をしているか」だけでなく「なぜそう設計したか」を冒頭のモジュールdocstringに書く形式で統一されている。新規ファイルもこれに倣う。
- 変更時は周囲のコードの粒度・命名・コメント密度に合わせる。独自の書式を持ち込まない。
- 型ヒントを付ける。`TypedDict` によるState定義は `graph_agent.py` の形式に合わせる。
- 使い捨てスクリプト（`test_llm.py`、`list_models.py` など）はその旨をdocstringに明記する。
- ゲートの成果物（`gate2_findings.md` 〜 `gate5_blog_draft.md`）は**学習の記録であり、後から都合よく書き換えない**。数値を更新する場合は、いつ・何を変えて再測定したかを併記する。

---

## 数値の扱い（ゲート4以降）

- 精度・コスト・レイテンシは**必ず実測して書く**。推定値を断定形で書かない。推定しか出せない場合は「推定」と明記する。
- モデル比較は最低2種類。`gate4_findings.md` の表と同じ列（成功率／LLM呼び出し平均レイテンシ／合計レイテンシ平均／平均トークン数）を維持する。
- 「成功」の定義を変えたときは、定義の変更を先に明記してから数値を出す。定義を変えたまま旧数値と並べて比較しない。

---

## Claudeのロール（作業モード）

1つのタスクに複数が関わる場合は、どのロールとして動くかを明示してから作業する。

### 1. エージェント設計者
- LLM判断／決定的処理の切り分け、ワークフロー分解、承認ゲートの配置
- **判断範囲**：設計案の提示と、案ごとのトレードオフ整理まで。採用の決定はTodoが行う。

### 2. 評価担当
- 評価データセットの追加案、成功条件の粗い定義、計測スクリプトの実装、結果の集計
- **判断範囲**：測定と集計まで。「この数値なら十分」という合否判断はしない。

### 3. エラー分析担当（ゲート2）
- 失敗ケースの収集、原因分類（データ／検索・取得／プロンプト／モデル／ツール設計）
- **判断範囲**：分類と傾向の提示まで。どの原因から潰すかの優先順位はTodoが決める。

### 4. 実装担当
- 既存の設計ルールに沿ったコード実装、テスト追加、リファクタ
- **判断範囲**：設計ルールの範囲内での実装。ルールから外れる変更は事前確認。

### 5. 説明担当（ゲート5）
- デモノート、技術ブログのドラフト、顧客向け説明（できること／できないこと／精度／コスト／人間の確認箇所）
- **判断範囲**：ドラフト提示まで。外部公開の可否と最終文言はTodoが決める。

---

## やらないこと

- 承認ゲート（`interrupt` / `[y/N]` 確認）の削除・迂回
- 実際にChatworkへ送信する・カレンダーに書き込むなど、テスト目的での外部への副作用の実行
- `.env`、APIキー、顧客の実名データを含む出力のコミット・外部送信
- ゲートの成果物ファイルやevalの生データの削除・上書き
- 「動かないので簡単な方に作り変える」タイプの変更（失敗は分析対象であって、回避対象ではない）
- テストのskip・無効化による「グリーン化」

---

## 未決定・保留中の項目

- `meeting_agent` / `travel_agent` のゲート2以降（失敗ケース30件・構造化・計測）が未着手。
- `meeting_agent` / `travel_agent` の `requirements.txt` を将来的に廃止するかどうか（現状は互換用に併存）。
- オブザーバビリティ（Langfuse / LangSmith）が未導入。現状の計測は `eval_run.py` の自前計測のみ。
- MCP接続が未実装（現状はGemini Function Callingのみ）。

---

## 作業を依頼されたときの手順

1. どのロールとして動くかを明示する
2. 該当するゲートの節（ルート `README.md`）と、`graph_agent.py` の設計方針を確認する
3. 上記「設計ルール」「やらないこと」に抵触しないか確認する
4. 決定事項と未決定事項を分けて整理する
5. 迷う点は選択式（2〜3択）で確認してから進める
