# 執務室（デモ版）

**クライアントが開く画面。** 契約したAI担当者が席にいて、そこに仕事を渡す。
「AI人材派遣」の体験（スキル別・入れ替え可・月額）を、そのまま画面にしたもの。

**https://todo2023.github.io/my-ai-agent/office/**

ビルド不要・サーバーなし・外部の読み込みなし。画像ファイルも使っていない
（似顔絵は `app.js` の中でSVGを組み立てている）。

## 社内用の道具とは別もの

| | 誰が見るか | 中身 |
| --- | --- | --- |
| [`office/`](.)（ここ） | **クライアント** | 担当者の顔・できること・料金。仕事を渡す入口 |
| [`../eigyo/`](../eigyo/) | **社内** | 担当者の中身（プロンプト）。調べ方と書き方の制限 |

**この2つは意図的に分けてある。** `office/staff.js` にプロンプトを書かないこと。
クライアントに配る画面に指示文が載ると、そのままコピーして使われる。

担当を1人増やすときは、両方に足すことになる。

| | 足す先 | 何を書くか |
| --- | --- | --- |
| 見せ方 | `office/staff.js` | 役割名、肩書、経歴、できること、できないこと、料金 |
| 中身 | `../eigyo/agents.js` | 調べる先、仮説の立て方、書き方の制限 |

## 画面でできること

1. **席を見る** … 3席。誰が座っていて、いま何をしているかが分かる
2. **担当を見る** … タップするとプロフィール。**できないことも必ず出す**
3. **担当を入れる／外す** … 空席に候補から選んで配属。合わなければ外せる
4. **仕事を頼む** … 商談を1件登録すると、その席に積まれる
5. **進み具合を見る** … 受付 → 準備中 → 納品済み

## いまはデモなので、ここが本物ではない

| | いま | 本番でどうするか |
| --- | --- | --- |
| 依頼の送信 | 送信しない。端末の中に残るだけ | こちらのメール／フォームに届くようにする |
| 状態の更新 | 画面の「進める」ボタンで手動 | こちらが進める。クライアント側にボタンは出さない |
| 納品物 | 出ない | メールで送る。ここにリンクを置くかは未定 |
| ログイン | ない。誰でも開ける | 会社ごとに分ける必要がある。**要検討** |
| 席数 | 3で固定 | 契約プランに合わせる |

**「進める」ボタンは、クライアントに見せる本番では消すこと。**
いまは動きを確かめるために出してある。

### つなぐときに考えること

保存先を持つ段になったら、無料枠の範囲で組めるか先に確認する。
`hp/match/` が Supabase の無料枠を使う予定なので、そちらと同じ置き場所にできるかどうか。
ログインが必要になった時点で、無料の構成では厳しくなる可能性がある。**その前に相談する。**

## 担当者の並び

| | 状態 | 月額 |
| --- | --- | --- |
| 商談準備 担当 | 契約中（初期配属） | 月4件 4万円 / 月8件 8万円 |
| オンコロジー 担当 | 候補（すぐ配属できる） | 月4件 5万円 / 月8件 10万円 |
| リサーチ 担当 | 準備中（選べない） | 未定 |
| コンテンツ 担当 | 準備中（選べない） | 未定 |

料金は仮の数字。`../hp/sales-ai/` の料金表と揃えてあるので、**片方だけ直さないこと。**
オンコロジー担当を1万円高くしてあるのは、確認の手間が増えるという想定を置いただけで、
根拠のある数字ではない。

## 経歴の書き方

`staff.js` の `career` には、**会社名・入社年・部署名を書かない。**

社名を伏せても、入社年と部署名まで揃うと人物が特定され得る。
いまは「大手製薬メーカーのオンコロジー領域で10年」まで落としてある。
`hp/README.md` の「実名について」と同じ考え方。

## 似顔絵

`app.js` の `avatar()` がSVGを組み立てている。`staff.js` の `color` と `mark` だけが担当ごとに変わる。

**いまの絵は仮。** 頭と肩と記号だけの簡単なもので、見せ方が固まったら描き直す前提。
外部の画像・フォント・3Dは読み込めない（無料構成を保つための決まり）ので、
描き直すときもSVGかCSSの範囲で作る。

## パソコンで確認する場合

```bash
python3 -m http.server 8000
# → http://localhost:8000/office/
```

保存した内容を消したいときは、ブラウザの開発者ツールで `localStorage` を空にする
（キーは `office.v1.seats` と `office.v1.jobs`）。

## 次にやること

- [ ] 似顔絵を描き直す（いまは仮）
- [ ] 「進める」ボタンを、本番向けには消せるようにする
- [ ] 会社ごとに分ける方法を決める（ログインが要るか、URLを分けるか）
- [ ] 依頼の送信先を決める（メール／フォームサービス／Supabase）
- [ ] 席数を契約プランに連動させる
- [ ] 納品物の受け取り方を決める
- [ ] リサーチ担当・コンテンツ担当の中身を決める（2本目以降の商品）
