# このリポジトリで作業するときの決まり

## 第一ルール：お金のかかることはしない

**この1条が、このファイルの他のどの決まりよりも上に立つ。**

**このリポジトリのものは、すべて無料の範囲で作る。お金は絶対にかけない。**

課金が発生する操作、または将来発生しうる操作をする前には、**必ず作業を止めて相談すること。**
勝手に進めてよい「小さな判断」には含めない。

### 課金の確認は「3回」

課金の相談をするときは、**同じ問いを3回する。3回とも「OK」と言われたときだけ検討に入る。**
1回でも「やめる」「保留」が出たら、その案は捨てて無料の方法を探す。

- 「安いから」「一時的だから」「あとで止められるから」は理由にしない
- 3回OKが出ても、それは**検討してよい**という意味であって、実行の許可ではない。
  実行の前にもう一度、何にいくらかかるかを出して確認する
- 迷ったら課金しない。**作らずに相談するほうが常に正しい**
- 方向性がどれだけ明確でも、課金の判断だけは自律的に進めない

### 相談なしにやってはいけないこと

- 有料プランへの変更・アップグレード（Supabase / Netlify / その他）
- クレジットカードの登録が必要なサービスの利用開始
- 独自ドメインの取得
- 従量課金のサービスを新しく構成に足すこと
- 無料枠の上限を超えることが分かっている作り（送信数・保存量・実行時間など）

無料枠の中で選択肢がないときは、**作らずに相談する**。
「とりあえず動くものを」で有料の構成にしない。

### 唯一の例外：Claude API

Claude API（Anthropic）だけは従量課金だが、事業の前提として承認済み。
ただし**実際にキーを使って動かす前に、費用の見積もりを出して確認を取る。**

参考単価（100万トークンあたり／**2026-08-29 に確認**。使う前に毎回、最新を確認する）

| モデル | 入力 | 出力 |
| --- | --- | --- |
| Opus 5 | $5 | $25 |
| Sonnet 5 | $2 | $10 |
| Haiku 4.5 | $1 | $5 |

※ Sonnet 5 は以前この表に $3 / $15 と書いていたが、確認したら値下がりしていた。
　 **表の数字を信じて見積もらないこと。使う前に必ず確かめる。**

Claude APIを使う実装では、次を必ず入れる。

1. **APIキーをブラウザに置かない**（漏れると第三者に使われ、上限なく課金される）
2. **自動実行・定期実行を作らない**。生成は手動で開始する形にする
3. **1回の実行で生成する件数に上限**を設ける
4. **`max_tokens` を明示**して、1回あたりの出力量を抑える
5. **毎回のトークン使用量（`usage`）を記録**して、あとから実費を確認できるようにする

## このリポジトリには2つのプロジェクトが入っている

フォルダで分かれていて、決まりの一部が違う。**お金の決まり（上）は両方に等しく効く。**

| | 何 | 言語・作り |
| --- | --- | --- |
| ① 配信するもの | アプリ置き場・会社サイト・コミュニティ（`biz/` `ehon/` `admin/` ほか） | HTML / CSS / JS。ビルドなし |
| ② AIエージェント教材 | `diet_agent` `invoice_agent` `meeting_agent` `travel_agent` `diet_gas` `diet_worker` | **Python** |

`main` ひとつで配信している（GitHub Pages は1ブランチしか配信できないため）。
**②を更新したときも `main` にマージしないと反映されない。**

### ② AIエージェント教材のほうの決まり

- 言語は Python。題材ごとにディレクトリを分ける
- モデルは Gemini API の無料枠（Google AI Studio のキー）を使う。
  レート制限（429）に当たる前提でリトライを入れる
- **APIキーは課金アカウントを紐付けない状態で使う。**
  無料枠を超えたら課金されるのではなく、エラー（429）で止まるようにしておく
- クレジットカード登録が必要なサービスは、無料枠に収まる見込みでも選ばない
  （Cloud Run、Fly.io など）
- APIキー無しでも動く確認手段（`demo.py` やダミー実装）を必ず用意する
- 外部への送信・削除など取り消せない操作の前には、人間の確認を挟む
- 進め方は [`README-agents.md`](README-agents.md) のゲート1〜5に沿う。
  各エージェントの現在地はそれぞれの README に書く

## 作るもの（2026-08-28に変えた前提）

**自分たちが使う道具だけを作る。一般向けに公開するものは作らない。**

いまの用途は**絵本ビジネスのSNS運用**。まず Instagram、
それがまわり出してから X。道具は [`insta/`](insta/) にある。

この変更で**対象外になったもの**（消さずに残してあるが、進めない）。

- `hp/`（会社サイト）の公開準備。会社概要の `（要記入）` も、問い合わせ先の設定も要らなくなった
- `hp/seo/` のダミーの導入事例の入れ替え
- 他人に使わせるサービス化（`hp/match/` のマッチング、`biz/` の投稿受け付けなど）

### ただし、次の2つは公開のまま

**非公開にするとお金がかかる。だからやらない。**

| | なぜ | |
| --- | --- | --- |
| 絵本（`ehon/`） | 「プロフィールのリンクから読めます」がSNS投稿の核 | |
| 投稿画像（`insta/images/`） | APIで投稿するとき Meta が公開URLから取りに来る | |

リポジトリごと非公開にすると、**GitHub Pages の配信は有料プラン限定**になり、
**GitHub Actions の無料枠にも上限**がつく。**現時点の費用ゼロが崩れる。**

作業用の画面（`insta/index.html` など）は `noindex` のまま。検索には出ない。

## いまの構成（2026年8月時点）

| | |
| --- | --- |
| 配信 | GitHub Pages（`main` をそのまま配信） |
| ビルド | なし。HTML / CSS / JS をそのまま置く |
| 外部の読み込み | なし（CDN・Webフォント等も使わない） |
| バックエンド | なし。各アプリは端末の中だけで完結する |

この構成なので、**現時点でかかっている費用はゼロ**。

例外として `hp/match/`（経営者・研究者マッチングの登録フォーム）だけは、
今後 Supabase（無料枠）と Claude API につなぐ予定がある。詳しくは `hp/match/README.md`。

`biz/`（ハタラク文庫）と `ehon/`（えほんの棚）は、コミュニティ作りの最初の形。
**いまは投稿を受け付けておらず、費用はゼロ。** 育て方と、どこから有料になるかは `sekkei/README.md`。
先の段（投稿・決済）に進むときは、**置き場所を GitHub Pages から移す必要がある**。
GitHub Pages は規約でECに使えないため。

## 会社としての役割定義は別ファイルにある

Claude Code に「合同会社To do の経営支援チーム」として動いてもらうときの
役割分担・報告フォーマット・未決事項は [`CLAUDE_team.md`](CLAUDE_team.md) に書いてある。
**このファイル（費用・構成の決まり）とあちらは両方が効く。**
食い違ったときは、**このファイルの費用ルールを優先する。**

## そのほか

- アプリは必ずフォルダに分ける（理由は `README.md` に書いてある）
- 個人情報を扱うページは、公開後も `noindex` を外さない
