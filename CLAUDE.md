# このリポジトリでの約束ごと

## 費用について（最優先）

**このリポジトリの成果物は、すべて無料で運用する。**

- 無料枠のあるサービスだけを使う。クレジットカード登録が必要なサービスは、
  たとえ無料枠内で収まる見込みでも**選ばない**（Cloud Run、Fly.io など）
- APIキーは課金アカウントを紐付けない状態で使う。無料枠を超えたら
  課金されるのではなく、エラー（429）で止まるようにしておく
- 無料枠の上限・スリープ・保存領域の消える条件は、採用時に必ず明記する

### 課金が必要になったとき

課金が避けられない、または課金の可能性がある選択肢を提案する場合は、
**3回に分けてユーザーに確認し、3回ともOKをもらってから**でなければ進めない。

1回目で断られた時点で終了。以降その案は提案しない。3回のうち1回でも
「やめる」があれば実行しない。無断でのカード登録・有料プラン加入・
Cloud Billing の有効化は、いかなる理由でも行わない。

## 開発の前提

- 言語は Python。エージェント題材ごとにディレクトリを分ける
  （`invoice_agent` / `meeting_agent` / `travel_agent` / `diet_agent`）
- モデルは Gemini API の無料枠（Google AI Studio のキー）を使う。
  レート制限（429）に当たる前提でリトライを入れる
- APIキー無しでも動く確認手段（`demo.py` やダミー実装）を必ず用意する
- 外部への送信・削除など取り消せない操作の前には、人間の確認を挟む

## カリキュラムとの対応

`README.md` のゲート1〜5に沿って進める。各エージェントの現在地は
それぞれの README に書く。
