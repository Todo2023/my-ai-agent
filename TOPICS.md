# AI Topics（ニュース欄）の書き足し方

公開先: `docs/topics.html`（**鍵をかけていません。**受講の有無にかかわらず誰でも読めます）

## なぜ講座と分けてあるか

資料ページ（`lessons.html` / `lesson.html`）は、提出した人にだけ次が開く仕組みです。
Topics はその逆で、**常時ぜんぶ読める**ようにしています。狙いは2つあります。

- 受講していない人にも、この講座が何を見ているかが伝わること
- ここに書いた問いを、**のちの回のワークのお題として引ける**ようにしておくこと

読めるようにする代わりに、**ここには教材の中身を置きません。**置くのは公開情報の要約と、
それに対する当社の見方だけです。

## 1件足す

`docs/topics.json` の `topics` に、次の形で足すだけです。HTMLは触りません。
並び順は気にしなくて構いません（`date` の新しい順に自動で並びます）。

```json
{
  "id": "2026-utokyo-college-of-design",
  "date": "2026-09-02",
  "tag": "教育",
  "title": "見出し",
  "lead": "何が起きたか。事実だけを短く。",
  "why": "講座としてなぜ取り上げたか。ここが当社の見方です。",
  "questions": ["この話から立てられる問い", "もう1つ"],
  "links": [{ "label": "出典の名前", "url": "https://..." }]
}
```

- `id` … そのニュースへの直リンク用（`topics.html#id` で開きます）。半角英数字とハイフンで
- `date` … `YYYY-MM-DD`。掲載日
- `tag` … 「教育」「制度」「技術」など短く
- `**強調**` … `title` `lead` `why` `questions` で効きます。HTMLは書かないでください
- `links` … `http(s)` 以外のURLは表示しません

## 書くときの約束

- **事実と見方を混ぜない。**起きたことは `lead`、当社の解釈は `why` に分けて書く
- 数字や固有名詞は、必ず出典を1つ以上つける。確認できないものは載せない
- `questions` は、**その場で答えが出ない問い**にする。調べれば分かることはワークになりません
- AIの新機能そのものより、**何に価値が置かれるようになるか**が見えるものを選ぶ

## ワークに引くとき

`questions` をそのまま、または少し直して、教材側（非公開リポジトリ `todo-curriculum`）の
`## ワーク` に置きます。Topics 側の記述は消さずに残してください。
受講者が出典まで遡れる状態にしておくためです。
